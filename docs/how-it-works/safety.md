# Knowing when *not* to cache

Caching a side-effectful or non-deterministic computation is worse than not caching it at all, so Cash runs a suite of static checks before it will store any result.

## The three detectors

## Analysis, then decision

## Annotations: your override

## Purity markers

## Try it: will this cache?
