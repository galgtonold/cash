# The decorator path

The `@cash.cache` decorator wires a Python function into the caching engine with no changes to the function body — a structured four-segment key uniquely identifies every call.

## The four-segment key

## The dependency-state hash

## The dependency graph

## Helpers you didn't decorate

## Hashing arguments

## The notebook bridge
