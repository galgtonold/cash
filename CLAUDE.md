# Claude Code instructions

The instructions for AI coding assistants in this repo live in **[`.github/copilot-instructions.md`](.github/copilot-instructions.md)**.

Read that file before starting any work. It covers:

- Project overview and current status
- Commit-message conventions (no `Co-Authored-By` trailers)
- Architecture, testing, and debugging conventions
- Critical conventions (cache-key unification, lineage system)
- The release process (auto-generated CHANGELOG from `git log`)
- Where to find roadmaps and planning docs

This file exists only to redirect — keep all conventions in `.github/copilot-instructions.md` so they stay in sync across Copilot, Claude Code, Cursor, and other agents.
