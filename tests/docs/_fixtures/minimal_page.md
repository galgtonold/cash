# Minimal Page

A lead paragraph.

```python
import cash
```

Some prose between fences.

```python
@cash.cache
def f(x):
    return x * 2
```

A bash fence that should be ignored:

```bash
cash inspect
```

Another python fence:

```python
result = f(10)
```
