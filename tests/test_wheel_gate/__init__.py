"""Makes this directory a package so its ``conftest`` is imported by its
qualified name, ``tests.test_wheel_gate.conftest``, and never clashes with
another directory's top-level ``conftest`` module."""
