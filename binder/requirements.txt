# Binder environment for examples/try_cash_binder.ipynb (the "Try cash live" demo).
# Pre-release: install cash from GitHub so the notebook launches ready to run.
# After the PyPI release, replace the git line below with:  cash-lib[pandas]
git+https://github.com/galgtonold/cash.git
# The feature-tour notebook uses these (pre-installed on Colab, not on Binder's
# minimal base image):
numpy
pandas
matplotlib
