# Binder environment for examples/try_cash_binder.ipynb (the "Try cash live" demo).
# cash-lib[pandas] pulls cash plus pandas (and numpy transitively) + pyarrow;
# matplotlib is used by the plotting cell and isn't part of any cash extra.
cash-lib[pandas]
matplotlib
